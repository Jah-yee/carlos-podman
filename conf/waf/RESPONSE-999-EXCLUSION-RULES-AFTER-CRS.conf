# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 CARLOS Contributors
# Site-specific ModSecurity CRS exclusion rules for the carlos-app WAF.
# Installed by carlos-ctl to $EMR_HOME/container/conf/waf/ ONLY if that file
# does not already exist — an existing (OpenO-era) exclusion file is kept
# as-is, since CARLOS serves the same OSCAR-lineage pages and the same
# false positives apply.
#
# Add SecRuleRemoveById / SecRuleUpdateTargetById exclusions below.
